import { WidgetPositionDirective } from './widget-position.directive';

describe('WidgetPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetPositionDirective();
    expect(directive).toBeTruthy();
  });
});
