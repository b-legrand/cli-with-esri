import { VisibleForScaleDirective } from './visible-for-scale.directive';

describe('VisibleForScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibleForScaleDirective();
    expect(directive).toBeTruthy();
  });
});
