import { MarkdownContentDirective } from './markdown-content.directive';

describe('MarkdownContentDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkdownContentDirective();
    expect(directive).toBeTruthy();
  });
});
